{
  "buildCommand": "pnpm --filter @workspace/dnet-market run build",
  "outputDirectory": "artifacts/dnet-market/dist/public",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
