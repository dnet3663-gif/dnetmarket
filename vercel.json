{
  "buildCommand": "pnpm install && pnpm run typecheck:libs && pnpm --filter @workspace/dnet-market run build",
  "outputDirectory": "artifacts/dnet-market/dist",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
