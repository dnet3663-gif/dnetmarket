{
  "buildCommand": "pnpm run typecheck:libs && pnpm --filter @workspace/dnet-market run build",
  "outputDirectory": "artifacts/dnet-market/dist",
  "installCommand": "npm install -g pnpm@9.15.9 && pnpm install --frozen-lockfile=false",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
